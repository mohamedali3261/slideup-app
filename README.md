# SlideUP - منصة العروض التقديمية الاحترافية

منصة متكاملة لإنشاء وتحرير العروض التقديمية بشكل احترافي مع دعم كامل للغة العربية.

## 🚀 المميزات

- ✨ محرر عروض تقديمية متقدم
- 🎨 مكتبة ضخمة من القوالب والأيقونات
- 🤖 مساعد ذكاء اصطناعي
- 📊 رسوم بيانية تفاعلية
- 🎬 أنيميشن وانتقالات سلسة
- 💾 حفظ تلقائي وسجل إصدارات
- 🌙 وضع داكن/فاتح
- 🌐 دعم كامل للغة العربية
- 📱 تصميم متجاوب
- 🔒 نظام أمان متقدم

## 📋 المتطلبات

- Node.js 18+ 
- npm أو yarn

## 🛠️ التثبيت

1. استنساخ المشروع:
```bash
git clone <repository-url>
cd slideup
```

2. تثبيت المكتبات:
```bash
npm install
```

3. إنشاء ملف `.env`:
```bash
cp .env.example .env
```

4. تعديل ملف `.env` وتغيير القيم:
```env
PORT=3001
JWT_SECRET=your-super-secret-jwt-key-here
ADMIN_PASSWORD=your-secure-admin-password
```

## 🚀 التشغيل

### Development Mode
```bash
# تشغيل Frontend و Backend معاً
npm run dev:all

# أو تشغيلهم بشكل منفصل:
npm run dev      # Frontend only (port 8080)
npm run server   # Backend only (port 3001)
```

### Production Mode
```bash
# بناء المشروع
npm run build

# تشغيل الإنتاج
npm start
```

## 📦 البناء للإنتاج

```bash
npm run build
```

سيتم إنشاء ملفات الإنتاج في مجلد `dist/`

## 🔐 الأمان

⚠️ **مهم جداً قبل الرفع للإنتاج:**

1. غيّر `JWT_SECRET` في ملف `.env` إلى قيمة عشوائية قوية
2. غيّر `ADMIN_PASSWORD` إلى كلمة مرور قوية
3. تأكد من عدم رفع ملف `.env` إلى Git
4. تأكد من عدم رفع مجلد `data/` (قاعدة البيانات)

## 🗄️ قاعدة البيانات

المشروع يستخدم SQLite مع Better-SQLite3:
- الملفات موجودة في مجلد `data/`
- يتم إنشاء قاعدة البيانات تلقائياً عند أول تشغيل
- حساب الأدمن الافتراضي: `admin` / `01021303309` (غيّره فوراً!)

## 📁 هيكل المشروع

```
slideup/
├── src/                    # Frontend React
│   ├── components/        # المكونات
│   ├── pages/            # الصفحات
│   ├── contexts/         # Context API
│   ├── hooks/            # Custom Hooks
│   ├── data/             # البيانات الثابتة
│   └── lib/              # المكتبات المساعدة
├── server/                # Backend Express
│   ├── index.ts          # Server الرئيسي
│   ├── db.ts             # إعدادات قاعدة البيانات
│   └── db-helpers.ts     # دوال مساعدة
├── data/                  # قاعدة البيانات SQLite
├── public/               # الملفات الثابتة
└── dist/                 # ملفات الإنتاج (بعد البناء)
```

## 🌐 الرفع على الإنتاج

### Vercel / Netlify
1. قم برفع المشروع على GitHub
2. اربط المشروع مع Vercel/Netlify
3. أضف متغيرات البيئة في لوحة التحكم
4. سيتم البناء والنشر تلقائياً

### VPS / Dedicated Server
1. انقل الملفات للسيرفر
2. ثبت المكتبات: `npm install --production`
3. ابنِ المشروع: `npm run build`
4. شغّل السيرفر: `npm start`
5. استخدم PM2 للتشغيل المستمر:
```bash
npm install -g pm2
pm2 start "npm start" --name slideup
pm2 save
pm2 startup
```

## 🔧 إعدادات إضافية

### Nginx Reverse Proxy
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

## 📝 الترخيص

جميع الحقوق محفوظة © 2025 SlideUP

## 🤝 المساهمة

المشروع خاص - للاستخدام الداخلي فقط

## 📞 الدعم

للدعم الفني، تواصل عبر نظام التذاكر داخل المنصة.
